$notes = $('#order_notes');
$notes.append('<%= j cell(OrderNote::Cell::Item, order_note).() %>');
$notes.scrollTop($notes[0].scrollHeight);
$('#order_note_content').val('');

$('#edit_order_<%= @order.id %>_comment td:last').replaceWith('<%= last_note(@order) %>');
