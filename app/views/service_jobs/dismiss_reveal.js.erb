// «Строгий ремонт»: ответ «Нет» обработан на сервере (маркер закрыт). Модалку
// уже сняли клиентски по клику; на всякий случай убираем идемпотентно. Вуаль
// остаётся снятой — технарь осознанно посмотрел.
$('#strict_repair_modal_<%= @service_job.id %>').remove();
