$('#bad_review_announcements')
  .attr('data-content', "<%= j render(collection: @announcements, partial: 'announcements/bad_review', as: :announcement) %>")
  .removeClass('hidden')
  .addClass('active');