$('#announce_link').replaceWith("<%= j header_link_to_announce %>");
$('#coffer_order_link').popover('hide');
<% if @announcements.present? %>
  <% @announcements.each do |announcement| %>
    <%#TODO implement via cable: publish_to '/announcements' do %>
//      $("#announcement_#{announcement.id}").remove();
    <%# end %>
  <% end %>
<% end %>

<% if @announcement.present? %>
  closeAnnouncement($("#announcement_<%= @announcement.id %>"));
<% end %>