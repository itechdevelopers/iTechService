// Ремонт снят с паузы → перерисовываем строку (кнопка → «Ремонт продолжен»).
$('#answered_approval_<%= @approval_request.id %>').replaceWith('<%= j render partial: 'answered_request', object: @approval_request, as: :approval_request %>');
