$('#client_conversations_list').html('<%= j render "list" %>');
// Адрес в строке браузера держим в согласии с показанным списком: иначе F5
// вернул бы вкладку по умолчанию, а не ту, что открыта сейчас.
//
// raw обязателен: .js.erb экранирует HTML так же, как обычный шаблон, и «&»
// между параметрами превратился бы в «&amp;» — после обновления страницы
// параметр звался бы «amp;department_id» и фильтр по филиалу слетал бы.
window.history.replaceState({}, '', '<%= raw j request.fullpath %>');
