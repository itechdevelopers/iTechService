.page-header
  = link_to t('.back'), client_conversations_path, class: 'btn pull-right'
  %h2
    = @conversation.contact_title
    %small= t(".channels.#{@conversation.channel}", default: @conversation.channel)

-# Карточка целиком лежит в партиале: взятие в работу и закрытие меняют и
-# шапку, и набор кнопок, и форму ответа — перерисовать блок дешевле, чем
-# точечно править четыре места из JS.
#client_chat_card= render 'card', conversation: @conversation, messages: @messages
