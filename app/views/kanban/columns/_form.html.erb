<%= simple_form_for(column, url: url, html: {class: 'form-horizontal'}) do |form| %>
  <%= form.input :name %>
  <%= form.input :position %>

  <div class="form-actions">
    <%= submit_button form %>
  </div>
<% end %>
