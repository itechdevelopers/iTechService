<%= simple_form_for(board, html: {class: 'form-horizontal'}) do |form| %>
  <%= form.input :name %>
  <%= form.input :background %>

  <div class="form-actions">
    <%= submit_button form %>
  </div>
<% end %>
