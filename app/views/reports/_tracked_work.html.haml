%table.table.table-hover.table-bordered
  %caption= 'Действия пользователей в рамках талона'
  %thead
    %th.center Талон
    %th.center Действия пользователя
    %th.center Ссылка
  %tbody
    - report.result[:users].each do |user|
      %tr.detailable
        %td{colspan: 3}= User.find(user[:user_id]).short_name
      - user[:audits].each do |audit|
        %tr.details
          %td= audit[:ticket_number]
          %td= audit[:action]
          %td= link_to 'Ссылка', audit[:link], target: '_blank'