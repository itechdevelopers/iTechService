<%= auto_header_tag %>

<%= simple_form_for @location, html: { class: 'form-horizontal' } do |f| %>
  <%= f.association :department %>
  <%= f.input :schedule %>
  <%= f.input :name, autofocus: true %>
  <%= f.input :code, collection: Location::CODES %>
  <%= f.input :position %>
  <%= f.input :hidden %>
  <%= f.input :less_popular %>
  <%= f.input :color, as: :string, input_html: { type: 'color', style: 'width: 60px; height: 30px; padding: 2px;' }, hint: 'Цвет локации для отображения в графиках' %>
  <%= f.input :storage_term, as: :radio_buttons, collection: [['3 месяца', 3], ['год', 12], ['больше года', 88]] %>
  <%= f.input :overstay_thresholds, hint: 'Через сколько дней уведомлять, например: 30, 40' %>

  <div class="form-actions">
    <%= submit_button f %>
  </div>
<% end %>
